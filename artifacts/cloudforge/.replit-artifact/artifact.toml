kind = "web"
previewPath = "/"
title = "CloudForge"
version = "1.0.0"
id = "artifacts/cloudforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22311

[services.development]
run = "pnpm --filter @workspace/cloudforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cloudforge", "run", "build" ]
publicDir = "artifacts/cloudforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22311"
BASE_PATH = "/"
